# Circle Screenshot - Landing Page

This folder contains the GitHub Pages landing page for Circle Screenshot Chrome Extension.

## 🌐 Deployment

This site is designed to be deployed via GitHub Pages.

### Setup Instructions:

1. **Push to GitHub:**
   ```bash
   cd circle-snip
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/circle-screenshot.git
   git push -u origin main
   ```

2. **Enable GitHub Pages:**
   - Go to your repo → Settings → Pages
   - Under "Source", select `main` branch
   - Select `/docs` folder
   - Click Save
   - Wait 2-3 minutes for deployment

3. **Your site will be live at:**
   ```
   https://YOUR_USERNAME.github.io/circle-screenshot/
   ```

## 📝 Before Going Live

Update these placeholders in the HTML files:

1. **Chrome Web Store URL:**
   - Replace `#CHROME_STORE_URL` with your actual Chrome Web Store listing URL
   - This appears in: `index.html`, `privacy.html`

2. **GitHub Username:**
   - Replace `yourusername` in Open Graph URLs
   - Update canonical URL

3. **Contact Email:**
   - Replace `support@example.com` with your actual email

## 📁 File Structure

```
docs/
├── index.html      # Main landing page (SEO optimized)
├── privacy.html    # Privacy policy page
├── styles.css      # All styles
├── README.md       # This file
└── images/
    ├── icon-16.png
    ├── icon-32.png
    ├── icon-48.png
    ├── icon-128.png
    ├── banner-hero.jpg
    ├── banner-effortless.jpg
    ├── banner-everything.jpg
    ├── banner-workflow.jpg
    └── banner-start.jpg
```

## 🔍 SEO Features

- Optimized meta title and description
- Open Graph tags for social sharing
- Twitter Card meta tags
- Structured data (JSON-LD) for rich snippets
- Semantic HTML5 structure
- Keyword-rich content
- Mobile responsive design

## 🎯 Target Keywords

- circle screenshot
- circular screenshot
- round screenshot
- circular capture
- round screen capture
- circle crop
- transparent PNG
- profile picture maker
- snipping tool alternative
